CREATE TABLE users (
    id INT, --- Int
    name VARCHAR(100) NOT NULL, --VARCHAR(n)
    email VARCHAR(255), --VARCHAR(n)
    age INT, -- Int
    active BOOLEAN DEFAULT TRUE -- Boolean
);

SELECT * from users



